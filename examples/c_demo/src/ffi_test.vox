@ffi_import("c", "strcmp")
fn strcmp(a: String, b: String) -> i32;

fn test_c_ffi_import_return_value_smoke() -> () {
  let eq: i32 = strcmp("vox", "vox");
  let ne: i32 = strcmp("vox", "lang");
  assert(eq == 0);
  assert(ne != 0);
}
