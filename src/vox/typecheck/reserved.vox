fn is_reserved_intrinsic_name(name: String) -> bool {
  if name.len() < 2 { return false; }
  return name.slice(0, 2) == "__";
}

fn is_std_module(mp: String) -> bool {
  if mp == "std" { return true; }
  if mp.len() < 4 { return false; }
  return mp.slice(0, 4) == "std/";
}
