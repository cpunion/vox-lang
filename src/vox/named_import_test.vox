import { assert as ok } from "std/testing"
import { one as uno, S, E } from "vox/named_import_dep"

fn test_named_imports_smoke() -> () {
  let s: S = S { x: uno() };
  let e: E = E.A(41);
  let v: i32 = match e { E.A(n) => n, E.None => 0, };
  ok(s.x == 1);
  ok(v == 41);
}
