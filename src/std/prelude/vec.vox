impl Vec[String] {
  fn join(v: Vec[String], sep: String) -> String {
    let mut out: String = "";
    let mut i: i32 = 0;
    while i < v.len() {
      if i != 0 {
        out = out.concat(sep);
      }
      out = out.concat(v.get(i));
      i = i + 1;
    }
    return out;
  }
}
