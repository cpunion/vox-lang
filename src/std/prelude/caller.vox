// Default caller context type used by callsite tracking helpers.
// A local `struct Caller` can still shadow this if a package needs custom shape.
pub struct Caller {
  pub file: String,
  pub line: i32,
  pub col: i32,
  pub module: String,
  pub func: String,
}
