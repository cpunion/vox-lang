# Pinned stage2 bootstrap release tag used by release pipeline.
# The release workflow fetches:
#   vox-lang-${STAGE2_BOOTSTRAP_TAG}-<platform>.tar.gz
# and extracts bin/vox-stage2[.exe] as rolling bootstrap compiler.
STAGE2_BOOTSTRAP_TAG="v0.1.2"
