# Pinned bootstrap release tag used by release pipeline.
# The release workflow fetches:
#   vox-lang-${BOOTSTRAP_TAG}-<platform>.tar.gz
# and extracts bin/vox[.exe] as rolling bootstrap compiler.
BOOTSTRAP_TAG="v0.1.2"
