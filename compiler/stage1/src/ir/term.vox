pub enum Term {
  Ret(bool, Value),         // has_val, val (dummy when !has_val)
  Br(String),               // target block name
  CondBr(Value, String, String), // cond, then, else
  Panic(Value),             // panic(message)
}
